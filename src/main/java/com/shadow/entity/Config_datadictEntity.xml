<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--数据字典-->
<mapper namespace="com.shadow.dao.Config_datadictDao">
    <!--数据字典(查询)-->
    <select id="config_datadictAll" parameterType="int" resultType="Config_datadictEntity">
         select * from config_datadict where config_id=#{config_id}
    </select>
</mapper>